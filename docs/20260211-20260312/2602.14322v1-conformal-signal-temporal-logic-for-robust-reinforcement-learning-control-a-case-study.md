---
title: "Conformal Signal Temporal Logic for Robust Reinforcement Learning Control: A Case Study"
title_zh: 用于鲁棒强化学习控制的共形信号时序逻辑：一项案例研究
authors: "Hani Beirami, M M Manjurul Islam"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.14322v1"
tags: ["query:uav-air-combat"]
score: 9.0
evidence: 具有安全规范的F-16航空机动强化学习控制
tldr: 本研究针对航空航天领域强化学习控制的安全性与鲁棒性问题，提出了一种结合信号时序逻辑（STL）与符合预测（Conformal Prediction）的新型屏蔽机制。通过在F-16飞行模拟器上训练PPO智能体执行空速调节任务，并引入符合STL屏蔽器实时过滤动作。实验表明，该方法在面对模型失配、噪声和突发干扰等极端压力场景时，能有效保证STL规范的满足，同时维持接近基准的控制性能，为自主飞行控制提供了更强的可靠性保障。
selection_source: fresh_fetch
motivation: 旨在解决强化学习在航空航天控制任务中难以保证形式化安全规范和应对复杂环境扰动的挑战。
method: 提出一种符合STL屏蔽器，利用在线符合预测技术对强化学习智能体的动作进行实时监控与过滤，以确保满足时序逻辑要求。
result: 在F-16基准测试中，该方法在标称和极端压力场景下均实现了极高的STL规范满足率，且性能优于传统规则屏蔽器。
conclusion: 将形式化规范监控与数据驱动的强化学习相结合，是提升自主飞行控制系统在挑战性环境下可靠性的有效途径。
---

## 摘要
我们研究了形式化时序逻辑规范如何增强航空航天应用中强化学习（RL）控制的安全性和鲁棒性。利用开源的 AeroBench F-16 仿真基准，我们训练了一个近端策略优化（PPO）智能体来调节发动机节气门并跟踪指令空速。控制目标被编码为信号时序逻辑（STL）需求，即在每次机动的最后几秒内将空速保持在规定范围内。为了在运行时强制执行该规范，我们引入了一种共形 STL 屏蔽器（shield），利用在线共形预测来过滤 RL 智能体的动作。我们在名义条件和涉及气动模型失配、执行器速率限制、测量噪声以及回合中途设定点跳变的严苛压力场景下，对比了三种设置：(i) PPO 基准，(ii) 带有经典基于规则的 STL 屏蔽器的 PPO，以及 (iii) 带有提议的共形屏蔽器的 PPO。实验表明，共形屏蔽器在保持接近基准性能的同时，确保了 STL 的满足，并提供了比经典屏蔽器更强的鲁棒性保证。这些结果表明，将形式化规范监控与数据驱动的 RL 控制相结合，可以显著提高挑战性环境下自主飞行控制的可靠性。

## Abstract
We investigate how formal temporal logic specifications can enhance the safety and robustness of reinforcement learning (RL) control in aerospace applications. Using the open source AeroBench F-16 simulation benchmark, we train a Proximal Policy Optimization (PPO) agent to regulate engine throttle and track commanded airspeed. The control objective is encoded as a Signal Temporal Logic (STL) requirement to maintain airspeed within a prescribed band during the final seconds of each maneuver. To enforce this specification at run time, we introduce a conformal STL shield that filters the RL agent's actions using online conformal prediction. We compare three settings: (i) PPO baseline, (ii) PPO with a classical rule-based STL shield, and (iii) PPO with the proposed conformal shield, under both nominal conditions and a severe stress scenario involving aerodynamic model mismatch, actuator rate limits, measurement noise, and mid-episode setpoint jumps. Experiments show that the conformal shield preserves STL satisfaction while maintaining near baseline performance and providing stronger robustness guarantees than the classical shield. These results demonstrate that combining formal specification monitoring with data driven RL control can substantially improve the reliability of autonomous flight control in challenging environments.

---

## 论文详细总结（自动生成）

### 论文结构化总结：用于鲁棒强化学习控制的共形信号时序逻辑

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：强化学习（RL）虽然能处理复杂的非线性动力学系统（如F-16战斗机控制），但其黑盒特性导致在航空航天等高可靠性领域缺乏**形式化的安全保证**。特别是在面对模型失配、传感器噪声和环境突变（分布偏移）时，传统的RL策略容易失效。
*   **研究背景**：F-16 气动设计具有不稳定性，微小扰动可能导致剧烈振荡。现有的安全RL技术（如奖励塑形或简单屏蔽）通常缺乏严格的概率安全边界。本文旨在通过结合**信号时序逻辑（STL）**和**共形预测（Conformal Prediction）**，为RL控制器提供一个具有统计保证的运行时安全过滤机制。

#### 2. 方法论：核心思想与关键技术
该方法的核心是构建一个**共形STL屏蔽器（Conformal STL Shield）**，其工作流程分为离线校准和在线监控两个阶段：
*   **核心思想**：利用共形预测为系统未来的状态提供一个“无分布假设”的预测区间，并在这个区间内评估STL规范的鲁棒性。如果预测的最坏情况可能违反安全规范，屏蔽器将介入并修正RL智能体的动作。
*   **关键技术细节**：
    *   **PPO策略训练**：使用近端策略优化（PPO）训练基础控制器，目标是跟踪空速设定点。
    *   **STL规范定义**：将安全目标编码为数学公式，例如 $G_{[T-W, T]} (|\frac{V_t - sp}{sp}| \leq \tau)$，表示在任务最后 $W$ 秒内，空速误差必须保持在 $\tau$ 范围内。
    *   **共形预测（CP）**：通过离线收集的校准数据，计算预测残差的 $(1-\delta)$ 分位数 $\hat{q}$。在运行时，预测器给出未来状态的点估计，并结合 $\hat{q}$ 形成一个包含真实状态的置信区间。
    *   **屏蔽机制（Shielding）**：在每个时间步，屏蔽器评估候选动作在预测区间内的“最坏情况鲁棒性”。若鲁棒性小于零，则从允许的动作空间（受变化率限制）中选择能使鲁棒性最大化的安全动作。

#### 3. 实验设计
*   **仿真环境**：使用高保真 Python 仿真器 **AeroBench F-16**。
*   **实验场景**：设计了四个渐进式的压力场景（S0-S3）：
    *   **S0 (Nominal)**：标准名义条件。
    *   **S1 (Mild)**：引入轻微速率限制和低观测噪声。
    *   **S2 (Moderate)**：引入节气门饱和、更高的噪声和延迟。
    *   **S3 (Strong)**：全压力堆叠，包括气动模型切换（Morelli模型）、执行器上限、强噪声及中途设定点突变。
*   **对比基准（Baselines）**：
    1.  **PPO Baseline**：原始强化学习策略。
    2.  **PPO + STL Shield**：带有传统基于规则的速率限制过滤器的PPO。
    3.  **PPO + Conformal Shield**：本文提出的结合共形预测的STL屏蔽方案。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如GPU型号、数量）或具体的训练时长。
*   **实现细节**：提到使用了开源的 `Gymnasium` 环境进行封装，代码已在 GitHub 开源，表明其计算需求在标准工作站可接受范围内。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个场景（S0-S3），每种方法分别进行了 **20 次独立的回合（Rollouts）** 实验。
*   **充分性评估**：
    *   **客观性**：通过引入“压力测试堆叠”（Stress Stack），模拟了现实中可能出现的多种非理想情况，实验设计较为严苛且具有代表性。
    *   **消融/对比**：对比了纯RL和传统屏蔽器，清晰展示了共形预测在处理不确定性时的优势。
    *   **局限性**：20次实验样本量虽然能反映趋势，但在统计严密性上（对于关键安全应用）仍有提升空间。

#### 6. 主要结论与发现
*   **安全性显著提升**：在最严苛的 S3 场景下，纯 PPO 的安全规范满足率仅为 **60%**，而加入共形屏蔽器后，满足率提升至 **95%**。
*   **性能权衡**：共形屏蔽器虽然在收敛速度上略慢于纯 PPO（因为更保守），但它能确保在系统受到严重扰动时依然维持在安全带内。
*   **鲁棒性验证**：实验证明，共形预测提供的分位数边界能够有效覆盖模型失配带来的预测误差，从而在不重新训练RL策略的情况下实现跨场景的鲁棒控制。

#### 7. 优点（亮点）
*   **统计保证**：相比于启发式安全方法，共形预测提供了无分布假设的有限样本覆盖保证。
*   **即插即用**：该屏蔽器作为一个独立模块，无需修改或重新训练底层的 RL 策略，具有很强的通用性。
*   **可解释性**：STL 提供了清晰的数学语义，使得安全要求易于理解和验证。

#### 8. 不足与局限
*   **状态空间受限**：本案例研究主要集中在二维状态（空速和发动机状态）的纵向控制，尚未扩展到全 6-DOF（六自由度）的复杂空战或特技飞行机动。
*   **计算开销**：在线进行短时域预测和鲁棒性优化会增加控制回路的延迟，论文未详细讨论该方法在高频控制（如 >100Hz）下的实时性表现。
*   **保守性**：共形预测的区间可能过于保守，导致在某些情况下牺牲了过多的控制性能（如响应速度）。

（完）
