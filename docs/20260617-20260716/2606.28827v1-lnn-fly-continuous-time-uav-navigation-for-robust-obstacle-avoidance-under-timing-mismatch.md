---
title: "LNN-Fly: Continuous-Time UAV Navigation for Robust Obstacle Avoidance under Timing Mismatch"
title_zh: LNN-Fly：时序不匹配下鲁棒避障的连续时间无人机导航
authors: "Yulin Huang, Shaojie Chen, Di Feng, Jiahao Wang, Ping Liu, Jianxiao Zou"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28827v1"
tags: ["query:upa"]
score: 9.0
evidence: 面向部署的连续时间无人机避障策略
tldr: "无人机在真实杂乱环境中因时序不匹配（过时观测、变速率控制）导致避障失败。LNN-Fly提出连续时间导航策略，融合动态规划启发更新、控制间隔条件化与自适应遗忘门，通过可微训练应对部署扰动。仿真中提升避障鲁棒性，零样本迁移真实四旋翼，室内跨频率飞行100%成功，机载推理低延迟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28827v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 614, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28827v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1550, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28827v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28827v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1689, \"height\": 1069, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28827v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 614, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28827v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 858, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28827v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 827, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28827v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 709, \"label\": \"Table\"}]"
motivation: 端到端无人机导航在仿真中敏捷，但部署时因模拟器差异、感知不规则和变速率控制，避障性能严重退化，尤其在杂乱环境易碰撞。
method: 提出LNN-Fly，结合动态规划启发的结构化递归更新、显式控制间隔Δt条件化及输入驱动遗忘门，用带扰动的可微rollouts训练连续时间策略。
result: "仿真中提升避障性能，容忍低控制频率、稀疏观测和周期抖动；零样本迁移真实四旋翼，室内20次飞行100%成功，机载CPU推理中值时延约2.5ms。"
conclusion: LNN-Fly面向部署的连续时间设计，有效克服时序失配，实现敏捷鲁棒的避障，为无人机在真实动态环境的安全导航提供了新方案。
---

## 摘要
端到端无人机导航在仿真中可实现令人印象深刻的敏捷性，但部署后其避障行为往往会退化，因为策略必须容忍仿真器失配、感知不规则和变速控制。这些影响在杂乱环境中尤为危险，因为过时的观测或短暂的控制不规则可能直接导致碰撞。我们提出LNN-Fly，一种面向部署、基于激光雷达的无人机避障连续时间导航策略。该策略结合了受动态规划启发的结构化循环更新、对经过控制间隔Δt的显式条件化，以及输入驱动的自适应遗忘门，可在临近危险时刷新过时的潜在状态，同时保持持续机动中的一致性。它使用包含部署相关感知和时序扰动的可微分展开进行训练。在仿真中，LNN-Fly在测试场景下提升了避障性能，并对降低的控制频率、稀疏观测和控制周期抖动表现出更好的容忍度。它还能从简化的可微分仿真器零样本迁移到实体四旋翼。在室内跨频真实测试中，系统在20次飞行中实现100%的成功率，策略推理在桌面图形处理器上的中位延迟为0.514毫秒，在机载中央处理器上约为2.5毫秒，机载P95延迟低于30毫秒。

## Abstract
End-to-end unmanned aerial vehicle (UAV) navigation can achieve impressive agility in simulation, yet its obstacle-avoidance behavior often degrades after deployment because the policy must tolerate simulator mismatch, sensing irregularity, and variable-rate control. These effects are especially dangerous in cluttered environments, where stale observations or short control irregularities can directly lead to collisions. We present LNN-Fly, a deployment-oriented continuous-time navigation policy for LiDAR-based UAV obstacle avoidance. The policy combines a dynamic-programming-inspired structured recurrent update, explicit conditioning on the elapsed control interval Δt, and an input-driven adaptive forgetting gate that refreshes stale latent state near hazards while preserving consistency during sustained maneuvers. It is trained with differentiable rollouts that incorporate deployment-relevant sensing and timing perturbations. In simulation, LNN-Fly improves obstacle-avoidance performance in the tested settings and shows better tolerance to reduced control frequency, sparse observations, and control-period jitter. It also transfers zero-shot from a simplified differentiable simulator to a physical quadrotor. In indoor cross-frequency real-world tests, the system achieves 100% success over 20 flights, while policy inference has a median latency of 0.514 ms on a desktop graphics processing unit (GPU) and about 2.5 ms on the onboard central processing unit (CPU), with onboard P95 latency below 30 ms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

这项研究聚焦于**端到端无人机导航策略在从仿真环境迁移到真实世界部署时所面临的鲁棒性挑战**。具体而言，尽管基于学习的无人机飞行策略在仿真中表现出色，但在真实杂乱环境中执行避障任务时，其性能会因以下两个耦合问题而显著下降：

*   **核心问题一：迁移不稳定的强避障能力**：现有策略多侧重于速度或轨迹优化，未将避障作为首要目标，导致在仿真器、感知系统或飞控栈发生变化时，避障行为不稳定，容易发生碰撞。
*   **核心问题二：对真实控制回路中时序不匹配的脆弱性**：真实机载计算机常因资源受限而出现控制周期不固定（如频率下降、抖动），这导致依赖于恒定时间间隔的循环策略的隐藏状态演化与实际物理运动失调，产生不安全指令，在近距离避障时尤为危险。

**整体含义**是，可靠的无人机导航需要一种**以部署为导向的连续时间策略**，它必须能够容忍上述的模拟器变化与时间不规律性，从而在真实世界的杂乱环境中实现鲁棒避障。

### 2. 论文提出的方法论

LNN-Fly框架的核心思想是构建一个**时序感知的、结构化的连续时间循环策略**，使其运行不再依赖固定的控制周期。技术细节如下：

*   **核心架构**：一个轻量级的编码器-循环核-控制器头结构。融合激光雷达点云特征与无人机状态向量（速度、航向等），输出6维控制命令。
*   **动态规划启发的结构化循环更新（核心机制）**：
    *   **设计动机**：将隐藏状态 \( h(t) \) 视为一个应始终与“感知条件化的局部目标”保持一致的潜在表示。
    *   **动力学方程**：通过定义一个局部一致性势能，推导出一个带有目标收缩项和刺激驱动遗忘项的连续时间微分方程：
        \[
        \frac{dh}{dt} = \omega(x, h) \odot (\kappa(x, h) - h) - \lambda(x) \odot h
        \]
        其中，\(\kappa\) 是局部目标，\(\omega\) 是收缩速度，\(\lambda\) 是自适应遗忘率。这种设计使隐藏状态演化被结构化为朝向局部目标的收缩，而非无约束的重写。
*   **显式时序条件化与离散化**：
    *   为了解决非固定时间间隔 \(\Delta t_k\) 问题，该方程在近似下得到了闭式解（Closed-form Solution），形成离散状态更新公式：
        \[
        h_{k+1} = \alpha_k \odot h_k + (1 - \alpha_k) \odot \frac{\omega_k}{\omega_k + \lambda_k} \odot \kappa_k
        \]
        \[
        \alpha_k = \exp(-(\omega_k + \lambda_k)\Delta t_k)
        \]
    *   这种形式使得网络参数（\(\kappa, \omega, \lambda\)）的生成直接受到经过的对数时间 \(\delta_k = \log(\Delta t_k + \epsilon)\) 的条件化，从而感知物理时间的流逝。
*   **输入驱动的自适应遗忘门**：遗忘参数 \(\lambda_k\) 由当前感官输入直接决定。在开阔或稳定区域，\(\lambda_k\) 较低以保持记忆（惯性运动）；当接近危险或环境突变时，\(\lambda_k\) 增高，主动“遗忘”陈旧状态，使控制器反应更灵敏。
*   **可微物理学习**：使用可微分模拟器（简化点质量模型）进行训练，采用BPTT。训练过程中，通过注入部署相关扰动（激光雷达噪声、异步感知、控制周期抖动、执行延迟等）来缩小仿真与现实差距，优化一个包含路径跟踪、安全避障、平滑性和正则化的复合损失函数。

### 3. 实验设计

实验旨在全面评估LNN-Fly的避障能力、迁移鲁棒性及时序容忍度。

*   **仿真场景**：使用**Gazebo+PX4软件在环仿真**，设计了三种静态杂乱场景，障碍物密度依次增加（半径、覆盖率、间距不同）。训练则在更简化的可微点质量模型中完成，从而引入**仿真器迁移**的挑战。
*   **基准（Benchmark）与对比方法**：
    *   **Standard CfC**：从文献复现的闭式连续时间网络基线，参数规模相当。
    *   **Diff-Fly**：基于可微物理的敏捷飞行策略。
    *   **EGO-Planner V2**：先进的模块化规划算法。
    *   **LSTM**：标准长短时记忆网络基线。
*   **评估指标**：成功率、路径效率、到障碍物的最小距离、低于安全阈值的距离比例。

### 4. 资源与算力

*   **硬件配置**：
    *   **训练与仿真测试**：工作站，搭载 Intel i7-13700 CPU 和 NVIDIA RTX 5080 GPU。
    *   **真实飞行（机载）**：仅使用 Intel Core Ultra 7 155H CPU。
*   **训练细节**：优化器为 AdamW，学习率 \(5 \times 10^{-4}\)，总迭代次数 **10,000 次**，展开步长 \(T=150\)，批次大小 128。
*   **推理时延**：
    *   桌面GPU上策略推理中位延迟仅为 0.514 ms。
    *   机载CPU上实现约 2.5 ms 的中位延迟，P95延迟低于 30 ms，满足实时性要求。

### 5. 实验数量与充分性

论文设计了较为系统和充分的实验来验证主张：

*   **主仿真基准测试**：在**3种不同难度**的场景下，分别在**5档不同最大速度（最高 5.0 m/s）** 下运行，每种条件下进行 **50次** 试验，确保了统计显著性。实验设计考虑到了迁移挑战，公平地将所有学习方法置于统一的训练和观测条件下。
*   **时序鲁棒性专项测试**：系统性地测试了**降低控制频率（30Hz 到 5Hz）**、**组合感知稀疏度（N=1,3,5）** 以及**控制频率抖动**等多种时序扰动组合，每种条件下20次试验，并设置了忽略真实 \(\Delta t\) 的消融模型（LNN-Fly Fixed Δt）来分离变量。这组实验充分证明了方法在极端时序压力下的优势。
*   **训练动态消融实验**：对比了 LNN-Fly、Diff-Fly、Standard CfC 以及**无自适应遗忘门的 LNN-Fly** 变体在 10,000 步训练中的表现，清晰揭示了结构化核心设计和自适应遗忘门的不同贡献。
*   **真实世界验证**：分别在**户外森林**（最高速 4.0 m/s）和**狭窄室内走廊**（2.0 m/s，30Hz 和 10Hz）两种差异巨大的真实环境中进行了零样本迁移测试，室内20次飞行100%成功率，强有力地证明了策略的泛化能力。

整体实验设计客观、公平，覆盖了仿真到真实、静态到动态时序等多种维度。

### 6. 论文的主要结论与发现

*   **LNN-Fly显著提升了仿真到真实的迁移稳定性与避障成功率**，在杂乱场景和高速度下，其综合表现优于所有对比基线。
*   **显式的时序条件化和自适应遗忘门是实现时序鲁棒性的关键**。仅采用连续时间形式而忽略真实 \(\Delta t\) 的模型，在时序剧烈变化时性能急剧下降；而 LNN-Fly 能够利用 \(\Delta t\) 输入动态适配其循环动力学，有效应对低控制频率、感知延迟和周期抖动。
*   **任务结构化的循环动力学设计优于通用循环网络**。动态规划启发的收缩结构和自适应遗忘机制相比 Standard CfC 和 LSTM，能在复杂、存在扰动的任务中学习到更有效的避障策略。
*   **轻量级架构可实现高效的机载部署与实时推理**，证明了该方法在真实资源受限平台上的实用性。

### 7. 优点

*   **问题导向明确，方法针对性强**：直面部署中普遍存在的“时序不匹配”核心痛点，提出的每一个设计组件（\(\Delta t\) 条件化、自适应遗忘门）都有明确的任务目的。
*   **理论启发的结构化设计**：将循环神经网络从“黑箱”时序处理转变为受动态规划局部一致性思想启发的“结构化收缩”过程，增加了模型行为的可解释性和物理相关性。
*   **全面的评估体系**：不仅评估了不同场景和速度下的性能，更系统性地对多种时序扰动进行压力测试，并结合消融实验证明了各模块的价值。
*   **部署导向的闭环验证**：从在简化仿真器中训练，到在高保真仿真器测试，最后到真实飞行，形成了完整的“训练-验证-部署”闭环，验证了框架的实用性和鲁棒性。

### 8. 不足与局限

*   **动态障碍物处理缺失**：当前系统未集成动态障碍物预测模块，论文明确指出在遭遇快速移动障碍物时成功率会大幅下降，限制了其在高度动态环境中的应用。
*   **高速飞行性能下降**：尽管在低速和中速下表现出色，但在高速档位（如 5.0 m/s）的成功率依然会下降，表明残余的离散化误差和执行器延迟在高动态下仍是挑战。
*   **训练仿真器简化**：训练在简化的点质量模型上进行，虽然证明了迁移能力，但可能错过一些高阶动力学效应，当面对更复杂的真实气动效应时可能需要进一步适配。
*   **实验场景相对静态**：尽管场景杂乱，但主要验证的是静态障碍物避障，与真实世界丰富的动态交互仍有差距。

（完）
