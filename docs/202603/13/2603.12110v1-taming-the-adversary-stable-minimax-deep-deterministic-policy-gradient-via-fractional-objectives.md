---
title: "Taming the Adversary: Stable Minimax Deep Deterministic Policy Gradient via Fractional Objectives"
title_zh: 驯服对手：通过分式目标实现稳定的极大极小深度确定性策略梯度
authors: "Taeho Lee, Donghwan Lee"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12110v1"
tags: ["query:uav-air-combat"]
score: 8.0
evidence: 用户策略与对抗性扰动之间的极大极小优化
tldr: 针对强化学习在外部扰动和模型不确定性下表现不稳定的问题，本文提出MMDDPG框架。该框架将训练建模为用户策略与对抗扰动策略间的极小极大博弈，并引入分式目标函数来平衡任务性能与扰动强度，从而稳定训练过程。实验证明该方法显著提升了策略在面对外部力和参数变化时的鲁棒性。
selection_source: fresh_fetch
motivation: 强化学习智能体在面临外部扰动和模型不确定性时，往往会出现性能不稳定或严重退化的问题。
method: 提出MMDDPG框架，利用极小极大优化博弈并引入分式目标函数来平衡任务表现与扰动强度。
result: 在MuJoCo环境下的实验结果显示，该方法在应对外部力扰动和模型参数变化时表现出更强的鲁棒性。
conclusion: 通过分式目标函数稳定对抗训练过程，可以有效提升连续控制任务中策略的抗干扰能力和可靠性。
---

## 摘要
强化学习（RL）在广泛的控制和决策任务中取得了显著成功。然而，当部署在存在意外外部扰动和模型不确定性的环境中时，RL 智能体往往表现出不稳定或性能下降。因此，确保在此类条件下的可靠性能仍然是一个关键挑战。在本文中，我们提出了极大极小深度确定性策略梯度（MMDDPG），这是一个用于在连续控制任务中学习抗扰动策略的框架。训练过程被建模为用户策略与对抗性扰动策略之间的极大极小优化问题。在该问题中，用户学习一个最小化目标函数的鲁棒策略，而对抗者则生成最大化该目标的扰动。为了稳定这种交互，我们引入了一个平衡任务性能和扰动强度的分式目标。该目标防止了过度激进的扰动，并促进了鲁棒学习。在 MuJoCo 环境中的实验评估表明，所提出的 MMDDPG 在应对外部力扰动和模型参数变化方面均显著提高了鲁棒性。

## Abstract
Reinforcement learning (RL) has achieved remarkable success in a wide range of control and decision-making tasks. However, RL agents often exhibit unstable or degraded performance when deployed in environments subject to unexpected external disturbances and model uncertainties. Consequently, ensuring reliable performance under such conditions remains a critical challenge. In this paper, we propose minimax deep deterministic policy gradient (MMDDPG), a framework for learning disturbance-resilient policies in continuous control tasks. The training process is formulated as a minimax optimization problem between a user policy and an adversarial disturbance policy. In this problem, the user learns a robust policy that minimizes the objective function, while the adversary generates disturbances that maximize it. To stabilize this interaction, we introduce a fractional objective that balances task performance and disturbance magnitude. This objective prevents excessively aggressive disturbances and promotes robust learning. Experimental evaluations in MuJoCo environments demonstrate that the proposed MMDDPG achieves significantly improved robustness against both external force perturbations and model parameter variations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Taming the Adversary: Stable Minimax Deep Deterministic Policy Gradient via Fractional Objectives》的深度结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：深度强化学习（RL）智能体在面对外部扰动（如传感器噪声、物理冲击）和模型不确定性（如仿真与现实的参数差异）时，性能往往会剧烈下降甚至崩溃。
*   **研究动机**：
    *   现有的**对抗强化学习（Adversarial RL）**通过引入一个“对抗者”来生成扰动，试图训练鲁棒策略。
    *   然而，直接的极大极小（Minimax）优化极不稳定：对抗者往往会生成过大且不切实际的扰动，导致训练过程难以收敛，用户策略无法有效学习。
    *   传统的鲁棒控制（如 $H_\infty$ 控制）虽然理论完备，但在高维连续控制任务中计算复杂度过高。

### 2. 论文提出的方法论：MMDDPG
*   **核心思想**：提出**极大极小深度确定性策略梯度（MMDDPG）**框架，通过引入一个**分式目标函数（Fractional Objective）**来平衡任务性能与扰动强度，从而“驯服”对抗者。
*   **关键技术细节**：
    *   **分式目标函数**：定义目标为 $J = J_1 / J_2$。其中 $J_1$ 是累积任务成本（用户想最小化，对抗者想最大化），$J_2$ 是累积扰动幅度的平方和。
    *   **对数变换**：为了简化梯度计算并增强数值稳定性，将分式优化转化为对数差值优化：$\min_\theta \max_\phi (\ln J_1 - \ln J_2)$。这使得梯度更新时自动包含了归一化项，防止梯度爆炸。
    *   **双 Critic 架构**：在 DDPG 基础上增加了一个 Critic 网络。$Q_{\psi1}$ 估计任务成本，$Q_{\psi2}$ 估计扰动强度。
    *   **算法流程**：
        1. 用户 Actor 尝试最小化 $Q_{\psi1}$。
        2. 对抗 Actor 尝试在最大化 $Q_{\psi1}$ 的同时，也增大 $Q_{\psi2}$（即在分式中作为分母，通过对数变换体现为减项），从而在挑战用户和限制自身扰动强度之间取得平衡。
        3. 使用 SARSA 风格的 TD 误差更新两个 Critic，并利用确定性策略梯度（DPG）更新两个 Actor。

### 3. 实验设计
*   **实验场景**：基于 MuJoCo 物理引擎的连续控制基准测试：
    *   **Reacher（达标器）**：2自由度机械臂到达随机目标。
    *   **Pusher（推球器）**：7自由度机械臂将物体推向目标点。
*   **扰动设置**：
    *   **外部力扰动**：在机械臂末端施加随机或有偏的高斯分布外力。
    *   **模型参数变化**：改变关节阻尼（Damping）和齿轮系数（Gear scale），模拟执行器性能退化或环境摩擦变化。
*   **对比方法（Benchmarks）**：
    *   **DDPG**：标准非鲁棒基线。
    *   **RARL**：经典的鲁棒对抗强化学习。
    *   **PR-DDPG / NR-DDPG**：动作鲁棒型 DDPG 变体（通过在动作空间注入噪声实现鲁棒性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号或数量。
*   **训练参数**：
    *   每个任务训练 100k 步（10万步）。
    *   网络结构为简单的 MLP（隐藏层 [256, 256]）。
    *   Batch size 为 128，使用 Adam 优化器。
    *   这种参数量级通常在单张消费级 GPU（如 RTX 3060）或高性能 CPU 上即可在数小时内完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   所有实验均使用了 **10 个独立随机种子**进行训练和评估，确保了统计意义上的显著性。
    *   在参数鲁棒性实验中，设计了 **5x5 的参数网格**（共25种配置），通过热力图展示了性能的平滑度。
*   **充分性评价**：实验设计较为充分。不仅对比了平均性能，还通过标准差（Error bars）展示了训练的稳定性。覆盖了“外部扰动”和“内部参数偏移”两个关键鲁棒性维度，客观地证明了 MMDDPG 在复杂任务（Pusher）中相对于 RARL 的优势。

### 6. 论文的主要结论与发现
*   **稳定性提升**：分式目标函数有效地防止了对抗者产生“毁灭性”的扰动，使得训练过程比传统的 RARL 更加平滑。
*   **鲁棒性增强**：MMDDPG 在面对未见过的外部力和参数失配时，其性能下降幅度远小于 DDPG 和动作鲁棒类方法。
*   **复杂任务优势**：在简单的 Reacher 任务中，RARL 尚能竞争；但在高维、长时程的 Pusher 任务中，MMDDPG 的优势非常明显，证明了该方法在处理复杂动力学时的扩展性。

### 7. 优点与亮点
*   **理论联系实际**：将控制理论中的 $H_\infty$ 范数思想巧妙地引入到深度 RL 的目标函数设计中，具有较强的理论支撑。
*   **无需精细调参**：相比于其他需要手动设置扰动范围或约束惩罚项的方法，分式目标通过结构设计自发地限制了对抗者，减少了超参数敏感性。
*   **端到端训练**：直接在目标函数层面解决鲁棒性，不需要额外的稳定性约束求解器。

### 8. 不足与局限
*   **正值假设**：对数变换要求 $J_1$ 和 $J_2$ 必须始终为正。虽然文中提到可以通过加偏置解决，但在奖励函数设计复杂的任务中，这可能需要额外的工程处理。
*   **环境局限**：实验仅限于 MuJoCo 仿真环境，尚未在真实的物理机器人或更复杂的视觉输入任务中验证。
*   **计算开销**：虽然比传统控制方法快，但相比标准 DDPG，它需要维护额外的 Critic 和 Actor 网络，增加了约一倍的内存和计算开销。

（完）
