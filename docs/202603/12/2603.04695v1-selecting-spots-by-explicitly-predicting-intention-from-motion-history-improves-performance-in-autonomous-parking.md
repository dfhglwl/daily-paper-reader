---
title: Selecting Spots by Explicitly Predicting Intention from Motion History Improves Performance in Autonomous Parking
title_zh: 通过从运动历史中显式预测意图来选择车位，可提升自动泊车的性能
authors: "Long Kiu Chung, David Isele, Faizan M. Tariq, Sangjae Bae, Shreyas Kousik, Jovin D'sa"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04695v1"
tags: ["query:uav-air-combat"]
score: 8.0
evidence: 通过运动历史预测意图以辅助决策
tldr: 本研究针对自动代客泊车（AVP）中的社交导航问题，提出了一种通过显式预测其他车辆意图来选择车位的流水线。该方法利用历史运动轨迹和概率信念图来推断他人的停车目标，并在包含遮挡和预测误差的真实仿真环境中进行了验证。相比于隐式模型或仅依赖短期预测的方法，该方案显著提升了泊车决策的准确性、社交接受度及任务成功率，证明了显式意图预测在复杂泊车环境中的必要性。
selection_source: fresh_fetch
motivation: 在自动代客泊车场景中，由于驾驶规则相对松散，准确预测其他车辆的长期停车意图对于实现安全且符合社交规范的决策至关重要。
method: 提出一种利用学习模型和概率信念图，直接从其他车辆的历史运动轨迹中显式预测其目标停车位的AVP决策流水线。
result: 仿真实验显示，该方法在预测准确性、社交接受度和任务完成率上均优于基于短期运动预测或隐式端到端模型的方法。
conclusion: 显式意图预测是处理泊车场景中多样且模糊的长期目标的关键，能够有效弥补短期运动预测在复杂决策中的不足。
---

## 摘要
在社交导航的许多应用中，现有研究表明，预测和推理人类意图可以帮助机器人代理做出更安全、更符合社交规范的决策。在这项工作中，我们针对自主代客泊车（AVP）研究了这一问题。在 AVP 场景中，自动驾驶主车代理必须放下乘客、探索停车场、寻找停车位、与其他车辆协商车位，并在无人监督的情况下完成泊车。具体而言，我们提出了一种 AVP 流水线，该流水线利用学习模型和概率信念图，通过从运动历史中显式预测其他代理的停车位置来选择停车位。为了测试该流水线，我们构建了一个包含反应式代理的仿真环境，并对主车代理进行了现实的建模假设，例如遮挡感知观测和非完美的轨迹预测。仿真实验表明，我们提出的方法优于现有的从未来预测运动中推断意图或将其隐式嵌入端到端模型的方法，在预测准确性、社交接受度和任务完成度方面取得了更好的结果。我们的核心见解是，在驾驶规则较为宽松的泊车场景中，显式意图预测对于推理多样且模糊的长期目标至关重要；这些目标无法仅凭短期运动预测可靠地推断，但可以从运动历史中有效地学习。

## Abstract
In many applications of social navigation, existing works have shown that predicting and reasoning about human intentions can help robotic agents make safer and more socially acceptable decisions. In this work, we study this problem for autonomous valet parking (AVP), where an autonomous vehicle ego agent must drop off its passengers, explore the parking lot, find a parking spot, negotiate for the spot with other vehicles, and park in the spot without human supervision. Specifically, we propose an AVP pipeline that selects parking spots by explicitly predicting where other agents are going to park from their motion history using learned models and probabilistic belief maps. To test this pipeline, we build a simulation environment with reactive agents and realistic modeling assumptions on the ego agent, such as occlusion-aware observations, and imperfect trajectory prediction. Simulation experiments show that our proposed method outperforms existing works that infer intentions from future predicted motion or embed them implicitly in end-to-end models, yielding better results in prediction accuracy, social acceptance, and task completion. Our key insight is that, in parking, where driving regulations are more lax, explicit intention prediction is crucial for reasoning about diverse and ambiguous long-term goals, which cannot be reliably inferred from short-term motion prediction alone, but can be effectively learned from motion history.

---

## 论文详细总结（自动生成）

这篇论文针对自动代客泊车（AVP）场景，探讨了如何通过显式预测其他车辆的意图来提升决策的安全性与社交接受度。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在自动代客泊车（AVP）中，车辆不仅要寻找车位，还需在规则较松散的停车场内与其他人类驾驶员“社交”。现有的社交导航方法在处理泊车问题时存在局限：要么仅依赖短期运动预测（难以推断长期目标），要么采用端到端隐式模型（缺乏可解释性且难以处理复杂博弈）。
*   **核心问题**：如何利用有限的观测信息，准确预测其他车辆的长期停车意图（即目标车位），并据此做出不冲突、符合社交礼仪的泊车决策？

### 2. 论文提出的方法论
该论文提出了一套完整的 AVP 流水线，核心思想是**“从运动历史中显式预测意图”**。
*   **概率信念图（Belief Map）与 BEV 重建**：由于传感器存在遮挡，主车（Ego）维护一个关于所有车位占用状态的概率图 $b_t$。通过当前观测更新地图，并对未观测区域进行“想象”重建，生成语义鸟瞰图（BEV），为意图模型提供输入。
*   **显式意图预测**：采用基于 CNN 的学习模型，输入车辆的运动历史、位置及重建的 BEV 图像，输出该车停入各个候选车位的概率 $\eta$。
*   **意图条件轨迹预测**：提出使用**三次贝塞尔曲线（Cubic Bézier curves）**。根据预测的意图目标点（车位中心）生成平滑轨迹，相比常速模型更能反映泊车时的复杂转向动作。
*   **决策与规划**：
    *   **泊车决策**：避开其他车辆意图概率高的车位（竞争规避）。
    *   **路径规划**：使用 Hybrid A* 算法，在考虑动态障碍物预测轨迹的前提下，寻找通往非竞争车位的路径。
    *   **探索与等待**：若无合适车位则进行探索，若路径受阻则原地等待。

### 3. 实验设计
*   **场景设置**：构建了一个包含 40 个车位、多条道路的仿真环境。
*   **代理模型**：引入了具有“被动性/反应性”（Passiveness）参数的代理，模拟不同谨慎程度的人类驾驶员。
*   **对比方法（Baselines）**：
    1.  **Explicit, Future [20]**：从未来预测轨迹中推断意图（对比“从历史推断”）。
    2.  **Implicit [23]**：端到端隐式推理意图（对比“显式推理”）。
    3.  **轨迹模型对比**：对比了常速模型、Hybrid A*、以及基于 Transformer 的学习模型。
*   **评估指标**：成功率、社交接受度（抢位率、干扰他人时长）、泊车耗时、计算效率、预测精度（minADE/minFDE）。

### 4. 资源与算力
*   **硬件环境**：实验在一台笔记本电脑上完成，配置为：
    *   **CPU**：24 核 Intel i9。
    *   **GPU**：NVIDIA RTX 4090 (Laptop)。
    *   **内存**：64 GB RAM。
*   **软件环境**：使用 Python 开发。
*   **训练/推理时长**：文中未明确提及意图模型的训练时长，但给出了推理阶段的耗时：车位选择（意图推理）约需 0.005s，而路径规划（Hybrid A*）约需 0.7s - 1.6s。

### 5. 实验数量与充分性
*   **实验规模**：针对“非反应性代理”和“反应性代理”各进行了 **500 组**随机场景模拟，总计 1000 组实验。
*   **充分性评价**：
    *   **客观性**：通过引入不同反应程度的代理，模拟了真实世界中的互动不确定性。
    *   **消融/对比**：对比了意图推理的来源（历史 vs 未来）和形式（显式 vs 隐式），实验设计较为全面。
    *   **局限性**：实验完全在仿真中进行，且场景相对单一（固定布局的停车场），未涉及多车型或极端天气/传感器噪声干扰。

### 6. 主要结论与发现
*   **显式预测更优**：显式预测意图的方法在成功率上比隐式或基于未来轨迹推断的方法高出约 9%。
*   **历史信息是关键**：在泊车这种长期目标明确但短期动作复杂的场景下，从运动历史学习意图比从短期预测轨迹中反推意图更准确。
*   **贝塞尔曲线的优势**：在轨迹预测中，以意图为条件生成的贝塞尔曲线在精度（minADE）和计算效率上达到了最佳平衡，优于复杂的学习模型或简单的常速模型。
*   **社交友好**：该方法显著降低了“抢车位”的频率，减少了对其他车辆正常行驶的干扰。

### 7. 优点
*   **BEV 重建创新**：提出了一种在部分观测下重建语义 BEV 的方法，使得现有的学习模型能应用于实际的局部感知场景。
*   **模块化设计**：流水线清晰，意图预测与路径规划解耦，方便替换更先进的规划器。
*   **社交度量**：不仅关注任务完成，还量化了“抢位”和“干扰”等社交指标，对 AVP 落地具有参考价值。

### 8. 不足与局限
*   **实时性瓶颈**：虽然意图预测很快，但 Hybrid A* 规划耗时较长（>1s），难以达到自动驾驶通常要求的 10Hz 实时频率。
*   **仿真偏差**：代理行为基于预设规则而非真实人类数据，可能无法完全覆盖人类驾驶员的非理性或极端博弈行为。
*   **泛化性限制**：意图模型依赖于特定的停车场地图结构，对于未地图化的区域或非标准停车位的处理能力尚待验证。
*   **代理类型单一**：目前未考虑行人、摩托车或正在驶离车位的车辆。

（完）
