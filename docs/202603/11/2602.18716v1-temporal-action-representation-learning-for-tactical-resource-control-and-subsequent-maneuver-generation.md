---
title: Temporal Action Representation Learning for Tactical Resource Control and Subsequent Maneuver Generation
title_zh: 用于战术资源控制和后续机动生成的时序动作表示学习
authors: "Hoseong Jung, Sungil Son, Daesol Cho, Jonghae Park, Changhyun Choi, H. Jin Kim"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18716v1"
tags: ["query:uav-air-combat"]
score: 8.0
evidence: 战术机动生成的时序动作表示
tldr: 针对自主机器人系统中资源控制与后续机动之间的因果依赖及多模态决策问题，本文提出了TART框架。该框架利用基于互信息目标的对比学习来捕捉资源使用与机动之间的时序依赖关系，并通过量化表示学习离散代码本，以引导策略生成具有时序连贯性的多模态行为。在迷宫导航和F-16空战模拟中的实验表明，TART在处理混合动作空间任务时显著优于现有基准。
selection_source: carryover_cache
motivation: 现有的混合动作空间方法未能充分捕捉资源使用与后续机动之间的因果依赖关系及战术决策的多模态特性。
method: 提出TART框架，通过对比学习捕捉时序依赖，并利用量化代码本表征循环战术模式以引导策略生成。
result: 在有限资源导航和高保真F-16空战模拟任务中，TART在资源利用效率和上下文感知机动生成方面均优于基准模型。
conclusion: TART通过学习时序动作表示，有效解决了复杂动态环境下受限资源的战术控制与协同机动生成问题。
---

## 摘要
自主机器人系统应当能够推理资源控制及其对后续机动的影响，特别是在能源预算有限或感知受限的情况下。基于学习的控制在处理复杂动力学方面非常有效，并将该问题表示为一个统一了离散资源使用和连续机动的混合动作空间。然而，以往关于混合动作空间的研究未能充分捕捉资源使用与机动之间的因果依赖关系。它们还忽略了战术决策的多模态特性，而这两者在快速演变的场景中都至关重要。在本文中，我们提出了 TART，这是一个用于战术资源控制和后续机动生成的时序动作表示学习框架。TART 利用基于互信息目标的对比学习，旨在捕捉资源-机动交互中固有的时序依赖关系。这些学习到的表示被量化为离散的代码本条目，用于调节策略，从而捕捉循环出现的战术模式，并实现多模态且时序连贯的行为。我们在两个资源部署至关重要的领域评估了 TART：(i) 迷宫导航任务，其中有限的离散动作预算提供了增强的机动性；(ii) 高保真空战模拟器，其中 F-16 智能体协同操作武器和防御系统与飞行机动。在这两个领域中，TART 始终优于混合动作基准模型，证明了其在利用有限资源和产生上下文感知的后续机动方面的有效性。

## Abstract
Autonomous robotic systems should reason about resource control and its impact on subsequent maneuvers, especially when operating with limited energy budgets or restricted sensing. Learning-based control is effective in handling complex dynamics and represents the problem as a hybrid action space unifying discrete resource usage and continuous maneuvers. However, prior works on hybrid action space have not sufficiently captured the causal dependencies between resource usage and maneuvers. They have also overlooked the multi-modal nature of tactical decisions, both of which are critical in fast-evolving scenarios. In this paper, we propose TART, a Temporal Action Representation learning framework for Tactical resource control and subsequent maneuver generation. TART leverages contrastive learning based on a mutual information objective, designed to capture inherent temporal dependencies in resource-maneuver interactions. These learned representations are quantized into discrete codebook entries that condition the policy, capturing recurring tactical patterns and enabling multi-modal and temporally coherent behaviors. We evaluate TART in two domains where resource deployment is critical: (i) a maze navigation task where a limited budget of discrete actions provides enhanced mobility, and (ii) a high-fidelity air combat simulator in which an F-16 agent operates weapons and defensive systems in coordination with flight maneuvers. Across both domains, TART consistently outperforms hybrid-action baselines, demonstrating its effectiveness in leveraging limited resources and producing context-aware subsequent maneuvers.

---

## 论文详细总结（自动生成）

这是一份关于论文《Temporal Action Representation Learning for Tactical Resource Control and Subsequent Maneuver Generation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：自主机器人在动态环境下（如空战、复杂导航）面临资源受限（如弹药、电池、传感器预算）的挑战。
*   **核心问题**：现有的混合动作空间（离散资源决策+连续机动控制）强化学习方法存在两个缺陷：
    1.  **因果依赖缺失**：未能充分捕捉离散资源使用决策与后续连续机动之间的时序因果关系。
    2.  **多模态性不足**：在相同战术背景下，智能体往往只能收敛到单一策略，无法产生应对复杂局势的多样化有效机动。
*   **整体含义**：本文旨在通过学习一种“时序动作表示”，将资源控制与机动生成耦合，使智能体能根据有限资源做出更具前瞻性和多样性的战术决策。

### 2. 方法论：TART 框架
*   **核心思想**：利用互信息（Mutual Information, MI）目标，通过对比学习将“状态历史+当前离散决策”与“未来连续机动序列”进行对齐，并将其量化为离散的战术代码本（Codebook）。
*   **关键技术细节**：
    *   **时序表示学习**：定义目标函数为最大化当前上下文（过去状态+当前离散动作）与未来 $K$ 步连续机动序列之间的互信息。
    *   **对比损失（InfoNCE）**：通过 InfoNCE 损失函数实现上述目标，将匹配的上下文-未来对拉近，不匹配的推开。
    *   **矢量量化（VQ）**：将连续的上下文嵌入映射到离散的代码本条目中。这些条目代表了不同的“战术模式”（Tactical Modes）。
    *   **因子化策略结构**：策略被分解为两步：首先由离散执行器决定资源使用；然后根据该决策和选定的战术代码，由连续执行器生成具体的机动动作。
    *   **训练流程**：采用 TART-PPO 算法，分为预热阶段（收集轨迹并训练表示模块）和主循环阶段（交替进行策略优化和表示学习）。

### 3. 实验设计
*   **实验场景**：
    1.  **受限资源迷宫导航**：智能体拥有有限的“加速（Boost）”和“穿墙（Penetration）”离散动作预算，需在复杂/动态迷宫中到达终点。
    2.  **高保真空战模拟（F-16）**：基于 JSBSim 动力学模型，智能体需管理有限的导弹和防御干扰弹，同时进行 6 自由度飞行控制。
*   **Benchmark 与对比方法**：
    *   **对比方法**：PADDPG、PDQN、HPPO（混合动作 PPO 基准）以及 HyAR（基于动作表示的先进混合动作 RL 方法）。
    *   **评估指标**：成功率（Success Rate）、到达目标时间（TTG/TTE）、路径覆盖率、单次击落耗弹量（SPE）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到迷宫任务使用了约 500 万参数的 ResNet 空间编码器；空战任务使用了 GRU-MLP 架构。模拟器运行频率为 10Hz。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个领域各设置了三个难度等级（Easy, Medium, Hard）。
    *   所有实验均基于 **5 个随机种子**取平均值，并标注了标准差。
    *   进行了**消融实验**，分别验证了对比损失（$L_{NCE}$）和量化损失（$L_{VQ}$）的必要性。
*   **充分性评价**：实验设计较为充分，涵盖了从简单静态环境到复杂动态对抗环境的过渡。通过消融实验证明了 TART 在处理“死锁”和“防御反击”等高级战术行为上的优越性，对比实验也选择了该领域主流的混合动作空间算法，具有较好的客观性。

### 6. 主要结论与发现
*   **性能领先**：TART 在所有场景下的成功率均显著优于基准模型，平均成功率达到 87.2%（对比最强基准 HyAR 的 77.4%）。
*   **资源效率**：在不增加资源消耗（甚至更少）的前提下，TART 实现了更高的任务成功率，证明其能更有效地利用有限的离散动作。
*   **多模态行为**：定性分析显示，TART 能够根据相同的输入产生多种有效的机动模式（如不同的避障路径或进攻姿态），这归功于 VQ 代码本的引入。

### 7. 优点
*   **时序耦合**：成功解决了离散决策与长程连续机动之间的脱节问题。
*   **战术可解释性**：通过离散的代码本条目，将复杂的机动抽象为可识别的战术模式。
*   **鲁棒性**：在动态障碍物（迷宫 Hard）和武装对手（空战 Hard）环境下表现出更强的适应能力。

### 8. 不足与局限
*   **环境局限**：目前仅在模拟器中验证，尚未在真实机器人硬件上进行测试。
*   **资源模型简化**：资源消耗被建模为离散的“次数预算”，而非现实中更复杂的连续物理成本（如随机动强度变化的燃料消耗）。
*   **计算开销**：虽然未详细列出，但引入对比学习和 VQ 模块会增加训练阶段的计算负担。
*   **应用限制**：对于不需要长程时序依赖的简单混合动作任务，该方法的优势可能不明显。

（完）
