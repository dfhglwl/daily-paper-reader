---
title: Event-Only Drone Trajectory Forecasting with RPM-Modulated Kalman Filtering
title_zh: 基于 RPM 调制卡尔曼滤波的纯事件无人机轨迹预测
authors: "Hari Prasanth S. M., Pejman Habibiroudkenar, Eerik Alamikkotervo, Dimitrios Bouzoulas, Risto Ojala"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01997v1"
tags: ["query:uav-air-combat"]
score: 9.0
evidence: 针对快速移动空中目标的纯事件无人机轨迹预测
tldr: 本研究针对高速无人机轨迹预测，提出一种仅利用事件相机数据的新方法。通过从原始事件流中直接提取螺旋桨转速（RPM）信息，并将其集成到RPM感知的卡尔曼滤波框架中，实现了对无人机运动状态的精确建模。该方法无需RGB图像或大量训练数据，在FRED数据集上的实验表明，其在短中期预测精度上显著优于现有的深度学习方法和传统卡尔曼滤波，为高速场景下的无人机感知提供了高效、鲁棒的解决方案。
selection_source: fresh_fetch
motivation: 针对高速移动无人机，现有轨迹预测方法对具有高时间分辨率优势的事件相机利用不足。
method: 从原始事件数据中提取螺旋桨转速（RPM）线索，并将其融合进RPM感知的卡尔曼滤波框架中。
result: 在FRED数据集上，该方法在0.4秒和0.8秒预测时界的误差表现均优于学习型方法和传统卡尔曼滤波。
conclusion: 实验证明，无需依赖RGB图像或训练数据，仅通过事件相机的运动特征即可实现准确的无人机中短期轨迹预测。
---

## 摘要
事件相机提供高时间分辨率的视觉感知，非常适合观察快速移动的空中物体；然而，其在无人机轨迹预测中的应用仍然有限。本研究提出了一种仅基于事件的无人机预测方法，该方法利用了螺旋桨引起的运动线索。螺旋桨转速直接从原始事件数据中提取，并融合在感知 RPM 的卡尔曼滤波框架中。在 FRED 数据集上的评估表明，所提出的方法在 0.4 秒和 0.8 秒预测时界的平均距离误差和最终距离误差方面，优于基于学习的方法和传统的卡尔曼滤波。结果证明，在不依赖 RGB 图像或训练数据的情况下，该方法能够实现鲁棒且准确的中短期轨迹预测。

## Abstract
Event cameras provide high-temporal-resolution visual sensing that is well suited for observing fast-moving aerial objects; however, their use for drone trajectory prediction remains limited. This work introduces an event-only drone forecasting method that exploits propeller-induced motion cues. Propeller rotational speed are extracted directly from raw event data and fused within an RPM-aware Kalman filtering framework. Evaluations on the FRED dataset show that the proposed method outperforms learning-based approaches and vanilla kalman filter in terms of average distance error and final distance error at 0.4s and 0.8s forecasting horizons. The results demonstrate robust and accurate short- and medium-horizon trajectory forecasting without reliance on RGB imagery or training data.

---

## 论文详细总结（自动生成）

### 论文总结：基于 RPM 调制卡尔曼滤波的纯事件无人机轨迹预测

#### 1. 核心问题与研究动机
*   **核心问题**：如何在非合作场景下，利用事件相机（Event Camera）的高时间分辨率特性，实现对高速、高机动无人机（UAV）的准确轨迹预测。
*   **研究动机**：
    *   **传统相机局限性**：RGB 相机在观察高速运动物体时存在运动模糊、帧率受限和高延迟问题，影响状态观测精度。
    *   **现有方法不足**：现有的深度学习（DL）预测方法依赖大量训练数据，且在未见过的无人机型号或飞行模式上泛化性差；而传统的运动学模型（如恒定速度模型）难以应对无人机剧烈的加减速和转向。
    *   **事件相机优势**：事件相机能以微秒级精度捕捉亮度变化，无运动模糊，非常适合提取无人机螺旋桨旋转等高频运动特征。

#### 2. 方法论
该方法不依赖深度学习，而是一种基于物理特征驱动的经典估计框架。
*   **核心思想**：从原始事件流中提取螺旋桨转速（RPM），并利用 RPM 及其变化率来动态调整卡尔曼滤波（KF）的预测噪声矩阵（Process Noise），从而使滤波器能自适应无人机的机动性。
*   **关键技术细节**：
    1.  **RPM 估计**：
        *   **频率分割**：在无人机边界框内构建空间频率图，通过百分位阈值（70th percentile）区分高频螺旋桨像素和低频机身像素。
        *   **周期直方图**：记录每个像素点事件的时间间隔，利用 100ms 的滑动窗口维护周期直方图，识别主导频率并转换为 RPM。
    2.  **运动模型**：采用离散时间恒定速度（CV）模型，状态向量包含中心位置 $(c_x, c_y)$ 和速度 $(v_x, v_y)$。
    3.  **RPM 调制过程噪声**：
        *   **逻辑**：高 RPM 通常对应高推力和剧烈机动，此时应增大过程噪声，使滤波器更信任实时观测值；低 RPM 对应平稳飞行，应更信任运动模型。
        *   **公式**：定义缩放因子 $\alpha_v = \max(0.5, 1 + 2r + \max(0, \dot{r}))$，其中 $r$ 是归一化 RPM，$ \dot{r}$ 是 RPM 的变化率。该因子直接作用于过程噪声协方差矩阵 $Q$。
    4.  **轨迹预测**：基于当前滤波状态，通过运动模型进行多步外推，预测未来 0.4s 和 0.8s 的位置。

#### 3. 实验设计
*   **数据集**：使用 **FRED (Florence RGB-Event Drone Dataset)**，这是目前最大的无人机 RGB-事件同步数据集，包含 14 小时以上的录像，涵盖雨天、弱光、室内、长距离等多种挑战场景。
*   **Benchmark（评估指标）**：平均位移误差（ADE）和最终位移误差（FDE），单位为像素（px）。
*   **对比方法**：
    *   **深度学习类**：LSTM、Transformer、CNN+Transformer（分别输入 RGB、事件或双模态）。
    *   **经典基准**：线性外推（Linear Extrapolation）、传统卡尔曼滤波（Vanilla KF）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **原因**：由于该方法是基于经典算法（卡尔曼滤波和直方图统计）的非学习型方法，**不需要进行模型训练**，其计算开销极低，主要涉及实时信号处理，理论上可在嵌入式 CPU 上运行。

#### 5. 实验数量与充分性
*   **实验规模**：在 FRED 数据集的测试集上进行了评估，涵盖了 54 个测试案例，涉及 5 种不同的无人机型号。
*   **充分性与公平性**：
    *   实验对比了多种最先进的深度学习架构，并使用了统一的输入（Ground Truth 边界框作为历史轨迹输入），确保了对比的公平性。
    *   展示了在不同极端环境（如夜间、雨天、高动态运动）下的定性结果，验证了鲁棒性。
    *   通过消融实验（对比 Vanilla KF 和 Kalman+RPM）证明了 RPM 调制机制的有效性。

#### 6. 主要结论与发现
*   **性能领先**：所提方法在所有指标上均优于深度学习方法。在 0.8s 预测时界下，其 FDE 显著低于 CNN+Transformer。
*   **经典方法胜过学习方法**：实验发现简单的线性外推和传统 KF 表现竟然优于复杂的深度学习模型，这表明深度学习模型在 FRED 数据集上可能存在严重的过拟合或泛化问题。
*   **RPM 调制的价值**：引入 RPM 调制后，相比传统 KF，0.8s 预测的 FDE 降低了约 4.2 像素，证明了物理特征对状态估计的提升作用。

#### 7. 优点
*   **无需训练**：作为非学习方法，它不需要标注数据进行训练，具有极强的即插即用能力和泛化性。
*   **实时性强**：算法复杂度低，适合部署在算力受限的边缘设备或反无人机系统中。
*   **物理可解释性**：利用螺旋桨转速与机动性的物理关联，比“黑盒”深度学习模型更具解释性。
*   **环境鲁棒性**：在 RGB 相机失效的弱光或高速场景下，依然能保持高精度。

#### 8. 不足与局限
*   **依赖检测器**：虽然实验使用了 GT 边界框，但在实际应用中，该方法高度依赖前端检测算法的准确性。
*   **模型假设**：恒定速度模型在极长时界的预测中可能失效，且目前仅针对多旋翼无人机（有可见螺旋桨），对固定翼无人机可能不适用。
*   **RPM 估计精度**：如果无人机距离过远导致螺旋桨像素过少，RPM 估计的准确度会下降，进而影响噪声调制的有效性。
*   **单一 RPM 假设**：目前将整个无人机视为单一 RPM 来源，未考虑四个旋翼转速不一致时的姿态变化信息。

（完）
